{
  "name": "server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "test": "vitest run",
    "postinstall": "prisma generate",
    "migrate": "prisma migrate deploy",
    "seed": "prisma db seed",
    "generate": "prisma generate",
    "bundle": "esbuild src/app.js --bundle --platform=node --format=esm --target=node22 --outfile=../api/_app.mjs --external:pg-native --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --log-level=warning"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.10.0",
    "@prisma/client": "^7.10.0",
    "bcryptjs": "^3.0.3",
    "cookie-session": "^2.1.1",
    "dotenv": "^18.0.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "nodemon": "^3.1.10",
    "prisma": "^7.10.0",
    "supertest": "^7.2.2",
    "vitest": "^4.1.11"
  }
}
